spring:
  datasource:
    spring.jpa.hibernate.ddl-auto=update
    spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/bankingservice
    spring.datasource.username=uthvfy
    spring.datasource.password=uthvfy
    spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
  #spring.jpa.show-sql: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: your-256-bit-secret
  expiration: 86400000

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG

server:
  port: 8080
